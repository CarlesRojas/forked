import Board from "@/component/Board";

const Game = async () => {
    return (
        <div className="relative h-full w-full p-6">
            <Board />
        </div>
    );
};

export default Game;
