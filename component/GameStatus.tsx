import { Match } from "@/game/match/type";

interface Props {
    match: Match;
}

const GameStatus = ({ match }: Props) => {
    return <div className="">GameStatus</div>;
};

export default GameStatus;
